name: Service quality CI
on: [push]
jobs:
  build-docker:
    runs-on: ubuntu-latest    
    steps:
      - uses: actions/checkout@v3
        with:
          path: app
      - run: cd app/ProjectPoc
      - run: docker build -f Dockerlile -t projectpoc:test .
      - run: cd ../IntegrationTest
      - run: scripts/Itest.sh